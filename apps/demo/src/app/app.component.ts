import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  standalone: true,
  selector: 'demo-root',
  template: 'hello',
})
export class AppComponent {
  title = 'demo';
}
